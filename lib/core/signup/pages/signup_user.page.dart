import 'package:flutter/material.dart';

class SignUpUserPage extends StatelessWidget {
  static String routeName = "/signup_user";
  const SignUpUserPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
