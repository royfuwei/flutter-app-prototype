import 'package:flutter/material.dart';

class NotificationSplashContent extends StatelessWidget {
  final String image;
  const NotificationSplashContent({
    Key? key,
    required this.image,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
